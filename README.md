# ML_Housing-_Prediction_
A machine learning project that predicts house prices using property features and location data. The project applies preprocessing with pipelines, encodes categorical variables, and compares multiple regression models using cross-validation and GridSearchCV to select the best model.
